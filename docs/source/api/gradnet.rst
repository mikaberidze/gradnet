GradNet (class)
===============

The core differentiable adjacency parameterization exposed at the top level of
``gradnet``.

.. currentmodule:: gradnet

.. autoclass:: GradNet
   :show-inheritance:
   :members:
   :member-order: bysource

.. note::

   Additional convenience methods (for example ``to`` or ``eval``) are inherited
   directly from ``torch.nn.Module``. Refer to the `PyTorch module documentation <https://pytorch.org/docs/stable/generated/torch.nn.Module.html>`_ for their full API.
